<div class="flex items-start p-4 mt-4 border rounded">
  <div class="flex-1 pl-4 flex items-start justify-between">
    <div class="leading-normal">
      <p class="text-gray-700 text-xs"><%= comment.user.name %> comentou <%= time_ago_in_words(comment.created_at) %> atrás</p>
      <div class="text-gray-900"><%= sanitize comment.reply %></div>
    </div>

    <% if author_of(comment) %>
      <%= link_to "Delete", meeting_comment_path(comment.meeting, comment), method: :delete, data: { confirm: "Tem certeza?" }, class: "btn btn-destroy" %>
    <% end %>
  </div>
</div> 